# Taskfile for tg-monitor-bot (https://taskfile.dev)
# Run `task --list` to see available tasks.
version: '3'

tasks:
  ship:
    desc: Safe production release (preflight → bump minor → multi-arch build & push → atomic helm upgrade w/ auto-rollback)
    cmds:
      - make ship
