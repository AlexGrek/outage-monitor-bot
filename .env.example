# Telegram Bot Configuration
# Optional: Leave empty to run in web-only mode (no Telegram notifications)
# Get from @BotFather: https://t.me/BotFather
TELEGRAM_TOKEN=your_bot_token_here

# Optional: Comma-separated list of allowed user IDs
# Leave empty to allow all users
ALLOWED_USERS=123456789,987654321

# Database Configuration
DB_PATH=data/state.db

# Monitoring Configuration
PING_COUNT=3
PING_TIMEOUT=5s
HTTP_TIMEOUT=10s
DEFAULT_CHECK_INTERVAL=30s

# Data Retention (30 days)
METRICS_RETENTION=720h

# REST API Configuration
API_ENABLED=true
API_PORT=8080
# Generate with: openssl rand -hex 32
API_KEY=your-secret-api-key-here
# Optional: Public base URL for incoming webhook links (e.g. https://outagemonitor.example.com)
# Set in Config in dashboard to show full webhook URLs for "Incoming Webhook" sources
# WEBHOOK_BASE_URL=https://outagemonitor.example.com

# Auto-Restart Configuration
AUTO_RESTART_ENABLED=true
# Initial delay before first restart
AUTO_RESTART_DELAY=30s
# 0 = unlimited, or set max attempts
AUTO_RESTART_MAX_ATTEMPTS=0
# Exponential backoff multiplier
AUTO_RESTART_BACKOFF_MULTIPLIER=2.0
# Maximum delay cap
AUTO_RESTART_MAX_DELAY=5m
